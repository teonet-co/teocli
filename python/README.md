Install swig and header files and a static library for Python:

    sudo apt-get install -y swig python-dev

Build python module and example:

    ./make_python.sh

Execute example in Teonet examples folder

    ./teocli.py